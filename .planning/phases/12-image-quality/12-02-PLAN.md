---
phase: 12-image-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompt/options/image/image-use-case.options.ts
  - src/lib/prompt/validation.test.ts
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "Every image use case option has a suggests field with 3-8 dimensions"
    - "Suggests keys match actual work type question IDs (subject, composition, art_style, etc.) — NOT prefixed aliases"
    - "Suggests values are arrays of validated option IDs (image_subject:hero_product, etc.)"
    - "CI test validates all suggested option IDs reference registered options"
    - "Core visual dimensions (art_style, composition, lighting, color_palette, perspective, mood) are prioritized"
    - "Video suggests remain untouched"
  artifacts:
    - path: "src/lib/prompt/options/image/image-use-case.options.ts"
      provides: "Enriched suggests on all 18 image use case options"
      min_lines: 390
  key_links:
    - from: "image-use-case.options.ts suggests keys"
      to: "image-prompt.worktype.ts question IDs"
      via: "question ID naming convention"
      pattern: "\"(subject|composition|art_style|lighting|color_palette|mood|perspective|scene|aspect_ratio|detail_level|post_processing|time_season|constraints)\""
    - from: "image-use-case.options.ts suggests values"
      to: "actual option IDs in image option sets"
      via: "CI validation test"
      pattern: "image_subject:|image_composition:|image_art_style:|image_lighting:|image_color_palette:|image_mood:|image_perspective:|image_scene:|image_aspect_ratio:|image_detail_level:|image_post_processing:|image_time_season:"
---

<objective>
Fix and enrich the `suggests` field on all 18 image use case options. Fix the suggests keys from incorrect `image_*` prefixed aliases to match actual work type question IDs. Enrich each use case's suggests to cover 4-8 core visual dimensions (prioritizing art_style, composition, lighting, color_palette, perspective, mood per D-05). Add CI validation that every suggested option ID references a real registered option.

Purpose: When a user selects a use case (e.g., "产品图"), the system can highlight recommended options across core visual dimensions. This uses the same star badge + "推荐" label pattern as the video suggests system.
Output: All 18 use cases with enriched suggests using correct question ID keys + CI validation test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-image-quality/12-CONTEXT.md
@src/lib/prompt/work-types/image-prompt.worktype.ts
@src/lib/prompt/options/image/image-use-case.options.ts (current state)
@src/lib/prompt/options/image/index.ts

<interfaces>
<!-- Key context for the executor -->

The image_prompt work type defines these question IDs (from image-prompt.worktype.ts):
```
use_case, subject, scene, composition, lighting, art_style, constraints,
color_palette, mood, perspective, aspect_ratio, detail_level, 
post_processing, time_season
```

These are the valid suggests KEY names. Currently the use case options INCORRECTLY use keys like `image_subject`, `image_composition` etc. These must be fixed to `subject`, `composition`, etc.

Option ID naming convention: `{optionSetId}:{optionId}`. The option set IDs for image catalogs are:
- image_use_case, image_subject, image_scene, image_composition
- image_lighting, image_art_style, image_color_palette
- image_mood, image_perspective, image_aspect_ratio
- image_detail_level, image_post_processing, image_constraints
- image_time_season

Video use case options follow the CORRECT convention. From src/lib/prompt/options/use-case.options.ts:
```typescript
suggests: {
  subject: ["subject:food_drink"],        // key = question ID "subject"
  scene: ["scene:warm_cafe_counter"],     // key = question ID "scene"
  // ...
}
```

The image suggests must follow the same pattern — keys are question IDs, values are `image_*` prefixed option IDs.

Core visual dimensions per D-05 (prioritize these): art_style, composition, lighting, color_palette, perspective, mood

Additional dimensions per D-05 (use when use case strongly implies): subject, scene, aspect_ratio, detail_level, post_processing, time_season

D-06: Same UX pattern as video suggests — star badge + "推荐" label, no auto-select. The Phase 12 work ensures data correctness. The Phase 13 UI will render the highlights.

D-07: Suggested option IDs MUST reference actually-registered image option IDs. Validation at CI time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix suggests keys + enrich suggests on all 18 image use case options</name>
  <files>src/lib/prompt/options/image/image-use-case.options.ts</files>
  <action>
**Step 1: Fix all existing suggests keys** from `image_*` prefixed aliases to actual work type question IDs.

Replace pattern:
```typescript
suggests: {
  image_subject: [...]       // WRONG - "image_subject" is an optionSetId, not a questionId
  image_composition: [...]   // WRONG
  image_art_style: [...]     // WRONG
  // etc.
}
```

With correct pattern (matching video suggests convention):
```typescript
suggests: {
  subject: [...]       // correct - "subject" is the work type question ID
  composition: [...]   // correct
  art_style: [...]     // correct
  // etc.
}
```

The values (option IDs like `["image_subject:hero_product"]`) remain unchanged — only the keys need fixing.

**Step 2: Enrich suggests for all 18 use cases per D-04 and D-05.**

Use the real option IDs from the image option catalogs. For each use case, select 4-8 dimensions with 1-3 suggestions per dimension. Focus on the core visual dimensions (art_style, composition, lighting, color_palette, perspective, mood), adding other dimensions where the use case strongly implies them.

**Complete suggests specification for all 18 use cases (new format with fixed keys + enriched content):**

| Use Case | subject | scene | composition | lighting | art_style | color_palette | mood | perspective | aspect_ratio | detail_level |
|----------|---------|-------|-------------|----------|-----------|---------------|------|-------------|-------------|-------------|
| poster | hero_product, single_person | | centered, symmetrical | dramatic_cross, cinematic | digital_painting, vector_flat, photorealistic | vibrant, complementary | bold_dramatic, confident_power | | portrait_2_3 | |
| banner | hero_product, single_person | | negative_space, centered | | | | | | landscape_16_9, ultra_wide_2_1 | |
| product_photo | hero_product | white_bg, studio_env | centered | studio_clean | product_photography, minimalist | muted, neutral | clean_professional | | square_1_1 | |
| avatar | single_person | solid_color | centered, fill_frame | window_soft, rembrandt | portrait_photography | neutral, muted | approachable_warm | | square_1_1 | |
| wallpaper | landscape_scene | natural_landscape, sky_cloud | rule_of_thirds | golden_hour, blue_hour | minimalist, digital_painting | pastel, muted | serene, hopeful_bright | | landscape_16_9 | |
| illustration | character_design, geometric_abstract | | | | digital_painting, watercolor, vector_flat | vibrant, pastel | playful_whimsical, serene | | | simplified, moderate |
| logo_icon | | | centered, negative_space | | minimalist, vector_flat | monochrome, complementary | clean_professional | | square_1_1 | simplified, minimalist |
| social_media_post | single_person, food_beverage | | fill_frame | window_soft | fashion_photography, film_photography | vibrant, warm | joyful_energetic, warm_cozy | | square_1_1, portrait_4_5 | |
| thumbnail | single_person, hero_product | | centered, fill_frame | high_key, hard_dramatic | photorealistic, digital_painting | vibrant, complementary | bold_dramatic, joyful_energetic | | landscape_16_9 | |
| ecommerce_main | hero_product | white_bg | centered | studio_clean | product_photography | neutral, muted | clean_professional | | square_1_1 | |
| greeting_card | | | centered, symmetrical | golden_hour, soft_dreamy | watercolor, digital_painting | warm, pastel | warm_cozy, joyful_energetic | | | |
| presentation_image | | | negative_space, centered | studio_clean | minimalist, vector_flat | sophisticated_gray, cool | clean_professional | | landscape_16_9 | simplified, moderate |
| article_hero | geometric_abstract, landscape_scene | | rule_of_thirds, negative_space | cinematic, golden_hour | digital_painting, photorealistic | sophisticated_gray, cool | bold_dramatic, mysterious | | landscape_16_9 | |
| packaging | hero_product | | centered, flat_lay | studio_clean, soft_dreamy | product_photography, minimalist | muted, pastel | clean_professional | | square_1_1, portrait_3_4 | high_detail |
| menu | food_beverage, plated_dish | indoor_setting | centered, flat_lay | soft_dreamy, warm_lamp | food_photography | warm, vibrant | appetizing_warm, warm_cozy | top_down | | |
| invitation | | | centered, symmetrical | soft_dreamy, candlelight | art_deco, minimalist, watercolor | jewel_tones, pastel | elegant_refined, warm_cozy | | | |
| print_material | hero_product | | centered | studio_clean | minimalist, photorealistic | sophisticated_gray, muted | clean_professional | | portrait_3_4, landscape_4_3 | hyper_detailed, high_detail |

**Detailed suggests for each use case** (write these directly into the option definitions):

1. **poster** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio (7 dims)
2. **cover_image** — subject, composition, color_palette, mood, aspect_ratio (5 dims)
3. **product_photo** — subject, scene, composition, lighting, art_style, color_palette, aspect_ratio (7 dims)
4. **avatar** — subject, scene, composition, lighting, art_style, color_palette, mood, aspect_ratio (8 dims)
5. **wallpaper** — subject, scene, composition, lighting, art_style, color_palette, mood, aspect_ratio (8 dims)
6. **illustration** — subject, art_style, color_palette, mood, detail_level (5 dims)
7. **logo_icon** — composition, art_style, color_palette, mood, aspect_ratio, detail_level (6 dims)
8. **social_media_post** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio (7 dims)
9. **thumbnail** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio (7 dims)
10. **ecommerce_main** — subject, scene, composition, lighting, art_style, color_palette, mood, aspect_ratio (8 dims)
11. **greeting_card** — composition, lighting, art_style, color_palette, mood (5 dims)
12. **presentation_image** — composition, lighting, art_style, color_palette, mood, aspect_ratio, detail_level (7 dims)
13. **article_hero** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio (7 dims)
14. **packaging** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio, detail_level (8 dims)
15. **menu** — subject, scene, composition, lighting, art_style, color_palette, mood, perspective (8 dims)
16. **invitation** — composition, lighting, art_style, color_palette, mood (5 dims)
17. **print_material** — subject, composition, lighting, art_style, color_palette, mood, aspect_ratio, detail_level (8 dims)
18. **greeting_card** (already covered as #11) ... wait, let me recount the 18 use case IDs:

List of all 18 use case option IDs from the file:
1. poster
2. cover_image
3. product_photo
4. avatar
5. wallpaper
6. illustration
7. logo_icon
8. banner
9. social_media_post
10. thumbnail
11. ecommerce_main
12. greeting_card
13. presentation_image
14. article_hero
15. packaging
16. menu
17. invitation
18. print_material

For **banner** (use case #8): subject, composition, aspect_ratio, color_palette, mood (5 dims)

**VERIFY**: Cross-reference each suggested option ID against actual option IDs in the image catalogs. Use `grep -o 'id: "image_[^"]*"'` on each catalog file to verify IDs exist before writing them.

**Implementation approach:**
1. First verify option IDs exist by grepping the catalog files
2. Then edit each use case option to replace the old `suggests` block with the enriched version
3. Keep all other fields (label, plain, professionalTerms, promptFragment, appliesTo, riskHint) unchanged
</action>
  <verify>
    <automated>npx vitest run --reporter=verbose src/lib/prompt/validation.test.ts -t "suggests"</automated>
  </verify>
  <done>
All 18 image use case options have enriched suggests with correct question ID keys; suggests cover 4-8 core visual dimensions per use case; existing suggests validation tests pass; tsc --noEmit passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CI validation that suggested option IDs reference real registered options</name>
  <files>src/lib/prompt/validation.test.ts</files>
  <behavior>
    - Test 1: For every image use case option with suggests, every value in every suggests array references an option ID that exists in the registered option sets
    - Test 2: A deliberately invalid suggests ID (e.g., "image_subject:nonexistent") causes the validation to fail
    - Test 3: All currently defined suggests values pass validation (no failures)
  </behavior>
  <action>
Add a new CI validation test to `src/lib/prompt/validation.test.ts` within the `suggests field validation (DIFF-03)` describe block. This test validates that every suggested option ID (the values) references a real registered option:

```typescript
it("all suggests values reference registered option IDs", () => {
  const allSets = getAllOptionSets();
  const allOptionIds = new Set(
    allSets.flatMap(set => set.options.map(o => o.id))
  );
  const invalid: string[] = [];
  
  for (const set of allSets) {
    for (const opt of set.options) {
      if (!opt.suggests) continue;
      for (const [questionId, suggestedIds] of Object.entries(opt.suggests)) {
        for (const suggestedId of suggestedIds) {
          if (!allOptionIds.has(suggestedId)) {
            invalid.push(
              `${opt.id}: suggests["${questionId}"] references unknown option "${suggestedId}"`
            );
          }
        }
      }
    }
  }
  expect(invalid).toEqual([]);
});
```

Also add a negative test:
```typescript
it("detects invalid suggests values", () => {
  const allOptionIds = new Set(
    getAllOptionSets().flatMap(set => set.options.map(o => o.id))
  );
  const testSuggests = {
    "image_subject:nonexistent": ["fake_option_that_does_not_exist"]
  };
  // Verify the fake ID is not in allOptionIds
  expect(allOptionIds.has("fake_option_that_does_not_exist")).toBe(false);
});
```

Update the existing suggests test "all suggests keys reference valid question IDs" — it currently uses `knownFutureImageQuestionIds` as a workaround. After fixing the keys in Task 1, the keys should match actual work type question IDs. The test should reference `resolveWorkType("image_prompt").questions.map(q => q.id)` directly without needing the `knownFutureImageQuestionIds` constant. Remove the `knownFutureImageQuestionIds` array and update the test to check against real image_prompt question IDs.
</action>
  <verify>
    <automated>npx vitest run --reporter=verbose src/lib/prompt/validation.test.ts -t "suggests"</automated>
  </verify>
  <done>
CI test validates all suggested option IDs are registered; deliberately invalid IDs fail validation; all 18 use cases pass; existing suggests tests pass with real question IDs (no knownFutureImageQuestionIds workaround); tsc --noEmit passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| option data → CI validation | Option data is authored in source files; CI validates structural correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | suggests data in use-case.options.ts | mitigate | CI validation checks every suggested ID references a real registered option |
| T-12-04 | Information Disclosure | suggests data | accept | No PII or sensitive data in option suggestions |
</threat_model>

<verification>
Run `npx vitest run` — all tests pass including suggests validation. Run `npx tsc --noEmit` — zero errors. Manual spot-check: open image-use-case.options.ts, verify 3 random use cases have correct suggests keys (e.g., `subject` not `image_subject`) and the suggested option IDs match actual catalog entries.
</verification>

<success_criteria>
- All 18 image use case options have enriched `suggests` with 4-8 dimensions each
- All suggests keys match actual work type question IDs (`subject`, `composition`, `art_style`, etc.)
- Core visual dimensions (art_style, composition, lighting, color_palette, perspective, mood) are prioritized per D-05
- CI validation test confirms every suggested option ID references a registered option
- Video use case options' suggests remain unchanged
- All existing suggests validation tests pass with updated question ID references
- `tsc --noEmit` reports zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-image-quality/12-02-SUMMARY.md`
</output>
