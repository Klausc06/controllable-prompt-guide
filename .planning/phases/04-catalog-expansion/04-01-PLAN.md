---
phase: 04-catalog-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompt/validation.ts
  - src/lib/prompt/validation.test.ts
  - src/lib/prompt/options/use-case.options.ts
  - src/lib/prompt/options/subject.options.ts
  - src/lib/prompt/options/scene.options.ts
  - src/lib/prompt/options/shot-type.options.ts
  - src/lib/prompt/options/camera-movement.options.ts
  - src/lib/prompt/options/style.options.ts
  - src/lib/prompt/options/lighting.options.ts
  - src/lib/prompt/options/motion.options.ts
  - src/lib/prompt/options/constraints.options.ts
  - src/lib/prompt/options/audio.options.ts
  - src/lib/prompt/options/format.options.ts
  - src/lib/prompt/options/text-handling.options.ts
  - src/components/prompt-guide.tsx
  - src/lib/prompt/targets/seedance.target.ts
  - src/lib/prompt/targets/generic-video.target.ts
  - src/lib/prompt/targets/veo3.target.ts
  - src/lib/prompt/reducer.test.ts
autonomous: true
requirements:
  - OPT-03
  - OPT-05
  - OPT-06

must_haves:
  truths:
    - "Every option ID uses catalogId:optionId namespace prefix format"
    - "All existing tests pass with prefixed option IDs"
    - "validateOptionIdFormat() returns empty array for all catalogs"
    - "No broken references — validateSafetyDefaultsIntegrity() returns empty"
    - "validateOptionIdsUnique() returns empty — no duplicate IDs across catalogs"
  artifacts:
    - path: "src/lib/prompt/validation.ts"
      provides: "validateOptionIdFormat() function"
      contains: "export function validateOptionIdFormat"
    - path: "src/lib/prompt/validation.test.ts"
      provides: "Unit test for namespace format validation"
      contains: "validateOptionIdFormat"
    - path: "src/lib/prompt/options/use-case.options.ts"
      provides: "Use-case options with prefixed IDs"
      min_options: 12
    - path: "src/lib/prompt/options/subject.options.ts"
      provides: "Subject options with prefixed IDs"
      min_options: 13
    - path: "src/lib/prompt/options/scene.options.ts"
      provides: "Scene options with prefixed IDs"
      min_options: 13
    - path: "src/lib/prompt/options/shot-type.options.ts"
      provides: "Shot type options with prefixed IDs"
      min_options: 9
    - path: "src/lib/prompt/options/camera-movement.options.ts"
      provides: "Camera movement options with prefixed IDs"
      min_options: 9
    - path: "src/lib/prompt/options/style.options.ts"
      provides: "Style options with prefixed IDs"
      min_options: 15
    - path: "src/lib/prompt/options/lighting.options.ts"
      provides: "Lighting options with prefixed IDs"
      min_options: 14
    - path: "src/lib/prompt/options/motion.options.ts"
      provides: "Motion options with prefixed IDs"
      min_options: 14
    - path: "src/lib/prompt/options/constraints.options.ts"
      provides: "Constraints options with prefixed IDs"
      min_options: 10
    - path: "src/lib/prompt/options/audio.options.ts"
      provides: "Audio options with prefixed IDs"
      min_options: 13
    - path: "src/lib/prompt/options/format.options.ts"
      provides: "Format options with prefixed IDs"
      min_options: 5
    - path: "src/lib/prompt/options/text-handling.options.ts"
      provides: "Text-handling options with prefixed IDs"
      min_options: 9
  key_links:
    - from: "src/components/prompt-guide.tsx defaults"
      to: "option IDs in catalog files"
      via: "string values in PromptSelections object"
      pattern: "use_case: \"use_case:gym_opening\""
    - from: "src/lib/prompt/targets/seedance.target.ts safetyDefaults"
      to: "constraints option IDs"
      via: "string array"
      pattern: "\"constraints:no_ip_or_celebrity\""
    - from: "src/lib/prompt/targets/generic-video.target.ts safetyDefaults"
      to: "constraints option IDs"
      via: "string array"
      pattern: "\"constraints:no_ip_or_celebrity\""
    - from: "src/lib/prompt/targets/veo3.target.ts safetyDefaults"
      to: "constraints option IDs"
      via: "string array"
      pattern: "\"constraints:no_ip_or_celebrity\""
    - from: "src/lib/prompt/validation.test.ts completeSelections"
      to: "option IDs in catalog files"
      via: "PromptSelections fixture values"
      pattern: "use_case: \"use_case:coffee_new_product\""
    - from: "src/lib/prompt/reducer.test.ts defaults"
      to: "option IDs in catalog files"
      via: "PromptSelections fixture values"
      pattern: "use_case: \"use_case:gym_opening\""
    - from: "validateOptionIdFormat()"
      to: "every option ID"
      via: "CI validation check"
      pattern: "startsWith(prefix)"
---

<objective>
Migrate ALL option IDs from bare strings to `{catalogId}:{optionId}` namespace prefix format (D-01). Add `validateOptionIdFormat()` CI guard. Update every reference across the codebase. Template map keys are question IDs and are NOT touched.

Purpose: Prevent cross-catalog option ID collisions. Make option IDs self-documenting. Enable the expansion in Plan 04-02 to proceed with the new convention already in place.

Output: 12 catalog files with renamed IDs, 5 reference files updated, 1 new validation function + test, all 58 tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-catalog-expansion/04-CONTEXT.md
@.planning/phases/04-catalog-expansion/04-RESEARCH.md

## Critical: What NOT to change

**templateMap keys are questionIds, NOT option IDs.** Do NOT touch any templateMap key in the 3 target files. These keys (use_case, subject, scene, motion, shot_type, camera_movement, lighting, style, audio, format, text_handling, constraints) remain unchanged.

**OptionSet.id values remain unchanged.** The catalog's own `id` field (e.g., `"format"`, `"use_case"`) is the set identifier and stays as-is. The namespace prefix is DERIVED from this field but the field itself does not change.

**No suppresses fields exist yet.** Verified by codebase grep. Do not add them.

## Catalog prefix map (derived from each catalog's `id` field)

| Catalog file | setId | Prefix |
|---|---|---|
| use-case.options.ts | use_case | use_case: |
| subject.options.ts | subject | subject: |
| scene.options.ts | scene | scene: |
| shot-type.options.ts | shot_type | shot_type: |
| camera-movement.options.ts | camera_movement | camera_movement: |
| style.options.ts | style | style: |
| lighting.options.ts | lighting | lighting: |
| motion.options.ts | motion | motion: |
| constraints.options.ts | constraints | constraints: |
| audio.options.ts | audio | audio: |
| format.options.ts | format | format: |
| text-handling.options.ts | text_handling | text_handling: |

## Reference update map

### prompt-guide.tsx defaults (15 value changes)
```typescript
use_case: "gym_opening"              → "use_case:gym_opening"
subject: "local_storefront"          → "subject:local_storefront"
scene: "bright_commercial_interior"  → "scene:bright_commercial_interior"
motion: "three_beat_story"           → "motion:three_beat_story"
shot_type: "medium_shot"             → "shot_type:medium_shot"
camera_movement: "slow_push_in"      → "camera_movement:slow_push_in"
lighting: "studio_clean"             → "lighting:studio_clean"
style: "cinematic_realism"           → "style:cinematic_realism"
constraints: ["no_ip_or_celebrity", "stable_identity", "readable_text"]
  → ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
audio: "upbeat_music"                → "audio:upbeat_music"
format: "vertical_10s"               → "format:vertical_10s"
text_handling: "short_title_only"    → "text_handling:short_title_only"
```

### Target safetyDefaults (7 value changes total)
- seedance.target.ts: `["no_ip_or_celebrity", "stable_identity", "readable_text"]` → `["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]`
- generic-video.target.ts: `["no_ip_or_celebrity", "stable_identity"]` → `["constraints:no_ip_or_celebrity", "constraints:stable_identity"]`
- veo3.target.ts: `["no_ip_or_celebrity", "stable_identity"]` → `["constraints:no_ip_or_celebrity", "constraints:stable_identity"]`

### validation.test.ts completeSelections (12 value changes)
```typescript
use_case: "coffee_new_product"           → "use_case:coffee_new_product"
subject: "food_drink"                    → "subject:food_drink"
scene: "warm_cafe_counter"               → "scene:warm_cafe_counter"
motion: "product_reveal"                 → "motion:product_reveal"
shot_type: "close_up"                    → "shot_type:close_up"
camera_movement: "slow_push_in"          → "camera_movement:slow_push_in"
lighting: "soft_daylight"                → "lighting:soft_daylight"
style: "warm_lifestyle"                  → "style:warm_lifestyle"
constraints: ["no_ip_or_celebrity", "stable_identity", "readable_text"]
  → ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
audio: "ambient_only"                    → "audio:ambient_only"
format: "vertical_10s"                   → "format:vertical_10s"
text_handling: "short_title_only"        → "text_handling:short_title_only"
```

### validation.test.ts inline option IDs (in test assertions)
- "simple_scene" → "constraints:simple_scene"
- "no_logo_hallucination" → "constraints:no_logo_hallucination"
- "food_drink" → "subject:food_drink"
- "warm_lifestyle" → "style:warm_lifestyle"

### reducer.test.ts defaults (6 value changes + inline assertions)
```typescript
use_case: "gym_opening"              → "use_case:gym_opening"
subject: "local_storefront"          → "subject:local_storefront"
scene: "bright_commercial_interior"  → "scene:bright_commercial_interior"
motion: "three_beat_story"           → "motion:three_beat_story"
constraints: ["no_ip_or_celebrity", "stable_identity", "readable_text"]
  → ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
format: "vertical_10s"               → "format:vertical_10s"
```

### reducer.test.ts inline option IDs in assertions
- "avoid_temporal_flicker" → "constraints:avoid_temporal_flicker"
- "person" → "subject:person"
- "single_focal_subject" → "constraints:single_focal_subject"
- "readable_text" → "constraints:readable_text"
- "local_storefront" → "subject:local_storefront"

## Existing code interfaces (from codebase)

From src/lib/prompt/types.ts:
```typescript
export interface OptionSet { id: string; version: string; label: LocalizedText; options: OptionItem[]; }
export interface OptionItem { id: string; version: string; label: LocalizedText; ... }
```

From src/lib/prompt/validation.ts:
```typescript
export function validateOptionIdsUnique(optionSets_?: OptionSet[]): string[];
export function validateSafetyDefaultsIntegrity(targets_?: TargetToolConfig[], optionSets_?: OptionSet[]): string[];
```

From src/lib/prompt/registry (via init.ts barrel):
```typescript
export function registerOptionSet(set: OptionSet): void;
export function getAllOptionSets(): OptionSet[];
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validateOptionIdFormat() to validation.ts + unit test</name>
  <files>src/lib/prompt/validation.ts, src/lib/prompt/validation.test.ts</files>

  <read_first>
@src/lib/prompt/validation.ts
@src/lib/prompt/validation.test.ts
@src/lib/prompt/types.ts
@.planning/phases/04-catalog-expansion/04-RESEARCH.md (validateOptionIdFormat section)
  </read_first>

  <behavior>
    - Test 1: validateOptionIdFormat([goodSet]) returns [] when all option IDs start with `setId + ":"`
    - Test 2: validateOptionIdFormat([badSet]) returns non-empty array when option ID has no namespace prefix
    - Test 3: validateOptionIdFormat([badSet]) error message contains the offending option ID
    - Test 4: validateOptionIdFormat([emptyPrefixSet]) catches option ID starting with bare `:` (empty namespace)
  </behavior>

  <action>
Implement `validateOptionIdFormat()` in validation.ts per the specification in RESEARCH.md (Migration Strategy section). The function:

1. Accepts optional `optionSets_?: OptionSet[]` parameter. If undefined, calls `getAllOptionSets()` (must import from registry).
2. For each option set, computes `prefix = set.id + ":"`.
3. For each option in the set, checks `option.id.startsWith(prefix)`. If not, pushes error: `Option "{option.id}" in set "{set.id}" must start with prefix "{prefix}"`.
4. Also checks `option.id.startsWith(":")` to catch empty namespace prefix. If true, pushes: `Option "{option.id}" in set "{set.id}" has empty namespace`.
5. Returns `string[]` (empty = all valid).

In validation.test.ts, add a `describe("validateOptionIdFormat")` block with 4 test cases matching the behavior spec above. Use inline OptionSet fixtures (no imports from catalog files — tests validate the function logic in isolation).

The function must be exported from validation.ts so it is importable by tests and CI.
  </action>

  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts -t "validateOptionIdFormat"</automated>
  </verify>

  <done>
validateOptionIdFormat() exists in validation.ts, exported. 4 tests pass: good set returns [], bad set with orphan ID returns error containing the ID, empty-namespace check catches bare colon. Full validation test suite continues to pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename all 136 option IDs in 12 catalog files to namespace prefix format</name>
  <files>
    src/lib/prompt/options/use-case.options.ts,
    src/lib/prompt/options/subject.options.ts,
    src/lib/prompt/options/scene.options.ts,
    src/lib/prompt/options/shot-type.options.ts,
    src/lib/prompt/options/camera-movement.options.ts,
    src/lib/prompt/options/style.options.ts,
    src/lib/prompt/options/lighting.options.ts,
    src/lib/prompt/options/motion.options.ts,
    src/lib/prompt/options/constraints.options.ts,
    src/lib/prompt/options/audio.options.ts,
    src/lib/prompt/options/format.options.ts,
    src/lib/prompt/options/text-handling.options.ts
  </files>

  <read_first>
@src/lib/prompt/options/use-case.options.ts
@src/lib/prompt/options/subject.options.ts
@src/lib/prompt/options/scene.options.ts
@src/lib/prompt/options/shot-type.options.ts
@src/lib/prompt/options/camera-movement.options.ts
@src/lib/prompt/options/style.options.ts
@src/lib/prompt/options/lighting.options.ts
@src/lib/prompt/options/motion.options.ts
@src/lib/prompt/options/constraints.options.ts
@src/lib/prompt/options/audio.options.ts
@src/lib/prompt/options/format.options.ts
@src/lib/prompt/options/text-handling.options.ts
@src/lib/prompt/validation.ts (verify validateOptionIdFormat exists)
  </read_first>

  <acceptance_criteria>
    - Every option `id` field in all 12 catalog files starts with the catalog's own `setId + ":"` prefix
    - The catalog-level `id` field (e.g., `id: "use_case"`) is UNCHANGED
    - No option ID exists without a namespace prefix in any catalog file
    - `npx vitest run src/lib/prompt/validation.test.ts -t "validateOptionIdFormat"` passes with real catalog data
    - TypeScript compilation succeeds (`npx tsc --noEmit`)
  </acceptance_criteria>

  <action>
**per D-01**: For each of the 12 catalog files, prepend the namespace prefix to every option's `id` field.

**Catalog-by-catalog rename — use the exact prefix map:**

1. **use-case.options.ts** — every option `id: "X"` → `id: "use_case:X"`
   - gym_opening → use_case:gym_opening, coffee_new_product → use_case:coffee_new_product, etc. (12 options)

2. **subject.options.ts** — every option `id: "X"` → `id: "subject:X"`
   - person → subject:person, food_drink → subject:food_drink, local_storefront → subject:local_storefront, etc. (13 options)

3. **scene.options.ts** — every option `id: "X"` → `id: "scene:X"`
   - bright_commercial_interior → scene:bright_commercial_interior, warm_cafe_counter → scene:warm_cafe_counter, etc. (13 options)

4. **shot-type.options.ts** — every option `id: "X"` → `id: "shot_type:X"`
   - medium_shot → shot_type:medium_shot, close_up → shot_type:close_up, etc. (9 options)

5. **camera-movement.options.ts** — every option `id: "X"` → `id: "camera_movement:X"`
   - slow_push_in → camera_movement:slow_push_in, etc. (9 options)

6. **style.options.ts** — every option `id: "X"` → `id: "style:X"`
   - cinematic_realism → style:cinematic_realism, warm_lifestyle → style:warm_lifestyle, etc. (15 options)

7. **lighting.options.ts** — every option `id: "X"` → `id: "lighting:X"`
   - studio_clean → lighting:studio_clean, soft_daylight → lighting:soft_daylight, etc. (14 options)

8. **motion.options.ts** — every option `id: "X"` → `id: "motion:X"`
   - three_beat_story → motion:three_beat_story, product_reveal → motion:product_reveal, etc. (14 options)

9. **constraints.options.ts** — every option `id: "X"` → `id: "constraints:X"`
   - no_ip_or_celebrity → constraints:no_ip_or_celebrity, stable_identity → constraints:stable_identity, readable_text → constraints:readable_text, simple_scene → constraints:simple_scene, no_logo_hallucination → constraints:no_logo_hallucination, no_extra_limbs → constraints:no_extra_limbs, single_focal_subject → constraints:single_focal_subject, avoid_temporal_flicker → constraints:avoid_temporal_flicker, avoid_quality_keywords → constraints:avoid_quality_keywords (10 options)

10. **audio.options.ts** — every option `id: "X"` → `id: "audio:X"`
    - upbeat_music → audio:upbeat_music, ambient_only → audio:ambient_only, etc. (13 options)

11. **format.options.ts** — every option `id: "X"` → `id: "format:X"`
    - vertical_10s → format:vertical_10s, vertical_15s → format:vertical_15s, square_8s → format:square_8s, horizontal_12s → format:horizontal_12s (4 options)

12. **text-handling.options.ts** — every option `id: "X"` → `id: "text_handling:X"`
    - no_text → text_handling:no_text, short_title_only → text_handling:short_title_only, placeholder_signage → text_handling:placeholder_signage, bottom_subtitle_bar → text_handling:bottom_subtitle_bar, opening_title_card → text_handling:opening_title_card, price_tag_popup → text_handling:price_tag_popup, end_card_cta → text_handling:end_card_cta, no_text_pure_visual → text_handling:no_text_pure_visual (8 options)

**CRITICAL: Do NOT change the catalog-level `id` field.** The `id: "format"` (OptionSet.id) stays as-is. Only the option-level `id` inside the `options: [...]` array gets the prefix.

**CRITICAL: Do NOT change templateMap keys.** The templateMap in target files uses question dimension IDs as keys — these are completely unrelated to option IDs.

After all 12 files are updated, run: `npx vitest run src/lib/prompt/validation.test.ts -t "validateOptionIdFormat"` to confirm all real options pass the format check.
  </action>

  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts -t "validateOptionIdFormat"</automated>
  </verify>

  <done>
All 136 option IDs in 12 catalog files have namespace prefix. Catalog-level `id` fields unchanged. validateOptionIdFormat() returns [] with real catalog data. TypeScript compiles. Tests that don't reference old-style IDs still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all 7 reference locations to use prefixed option IDs</name>
  <files>
    src/components/prompt-guide.tsx,
    src/lib/prompt/targets/seedance.target.ts,
    src/lib/prompt/targets/generic-video.target.ts,
    src/lib/prompt/targets/veo3.target.ts,
    src/lib/prompt/validation.test.ts,
    src/lib/prompt/reducer.test.ts
  </files>

  <read_first>
@src/components/prompt-guide.tsx
@src/lib/prompt/targets/seedance.target.ts
@src/lib/prompt/targets/generic-video.target.ts
@src/lib/prompt/targets/veo3.target.ts
@src/lib/prompt/validation.test.ts
@src/lib/prompt/reducer.test.ts
  </read_first>

  <acceptance_criteria>
    - Every option ID reference in prompt-guide.tsx defaults uses namespace prefix format
    - Every option ID in safetyDefaults arrays (3 target files) uses namespace prefix format
    - Every option ID in validation.test.ts fixtures uses namespace prefix format
    - Every option ID in reducer.test.ts fixtures and assertions uses namespace prefix format
    - templateMap keys in target files are UNCHANGED (they are question IDs)
    - `npx vitest run` — full test suite passes (58 tests)
    - `npx vitest run src/lib/prompt/validation.test.ts -t "safety"` passes (safetyDefaults integrity)
    - `npx vitest run src/lib/prompt/validation.test.ts -t "unique"` passes (no duplicate IDs)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <action>
**per D-01**: Update all option ID references that broke after Task 2 renamed the source-of-truth option IDs in catalog files. Process each file:

### 1. src/components/prompt-guide.tsx (lines 23-36)
Update the `defaults` object. Every bare option ID value must gain its namespace prefix:
```
use_case: "use_case:gym_opening"
subject: "subject:local_storefront"
scene: "scene:bright_commercial_interior"
motion: "motion:three_beat_story"
shot_type: "shot_type:medium_shot"
camera_movement: "camera_movement:slow_push_in"
lighting: "lighting:studio_clean"
style: "style:cinematic_realism"
constraints: ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
audio: "audio:upbeat_music"
format: "format:vertical_10s"
text_handling: "text_handling:short_title_only"
```

### 2. src/lib/prompt/targets/seedance.target.ts (line 17)
Change `safetyDefaults: ["no_ip_or_celebrity", "stable_identity", "readable_text"]` to:
`["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]`

### 3. src/lib/prompt/targets/generic-video.target.ts (line 17)
Change `safetyDefaults: ["no_ip_or_celebrity", "stable_identity"]` to:
`["constraints:no_ip_or_celebrity", "constraints:stable_identity"]`

### 4. src/lib/prompt/targets/veo3.target.ts (line 17)
Change `safetyDefaults: ["no_ip_or_celebrity", "stable_identity"]` to:
`["constraints:no_ip_or_celebrity", "constraints:stable_identity"]`

### 5. src/lib/prompt/validation.test.ts
Update `completeSelections` (lines 14-27):
```
use_case: "use_case:coffee_new_product"
subject: "subject:food_drink"
scene: "scene:warm_cafe_counter"
motion: "motion:product_reveal"
shot_type: "shot_type:close_up"
camera_movement: "camera_movement:slow_push_in"
lighting: "lighting:soft_daylight"
style: "style:warm_lifestyle"
constraints: ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
audio: "audio:ambient_only"
format: "format:vertical_10s"
text_handling: "text_handling:short_title_only"
```

Update inline option IDs in test assertions:
- Line 139: `"simple_scene"` → `"constraints:simple_scene"`
- Lines 297-301 (overMax): `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"`, `"stable_identity"` → `"constraints:stable_identity"`, `"readable_text"` → `"constraints:readable_text"`, `"simple_scene"` → `"constraints:simple_scene"`, `"no_logo_hallucination"` → `"constraints:no_logo_hallucination"`
- Lines 150, 153: `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"` in warning assertions
- Line 185: `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"` in warning assertion
- Line 218: `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"` in warning assertion
- Lines 284-285: `"food_drink"` → `"subject:food_drink"`, `"warm_lifestyle"` → `"style:warm_lifestyle"` in TEST-06 fragment assertions

### 6. src/lib/prompt/reducer.test.ts
Update `defaults` (lines 10-16):
```
use_case: "use_case:gym_opening"
subject: "subject:local_storefront"
scene: "scene:bright_commercial_interior"
motion: "motion:three_beat_story"
constraints: ["constraints:no_ip_or_celebrity", "constraints:stable_identity", "constraints:readable_text"]
format: "format:vertical_10s"
```

Update inline option IDs in assertions:
- Line 41: `"local_storefront"` → `"subject:local_storefront"`
- Line 42: `"vertical_10s"` → `"format:vertical_10s"`
- Line 51: `"avoid_temporal_flicker"` → `"constraints:avoid_temporal_flicker"`
- Line 61: `"avoid_temporal_flicker"` → `"constraints:avoid_temporal_flicker"`
- Line 66: `"readable_text"` → `"constraints:readable_text"` (in `new Set(...)`)
- Lines 74-75: `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"`, `"stable_identity"` → `"constraints:stable_identity"`
- Line 77: `"readable_text"` → `"constraints:readable_text"`
- Line 87: `"person"` → `"subject:person"`
- Line 90: `"person"` → `"subject:person"`
- Line 97: `"no_ip_or_celebrity"` → `"constraints:no_ip_or_celebrity"`, `"stable_identity"` → `"constraints:stable_identity"`, `"readable_text"` → `"constraints:readable_text"`, `"single_focal_subject"` → `"constraints:single_focal_subject"`
- Line 101: `"avoid_temporal_flicker"` → `"constraints:avoid_temporal_flicker"`
- Line 108: `"avoid_temporal_flicker"` → `"constraints:avoid_temporal_flicker"`
- Line 117: `"readable_text"` → `"constraints:readable_text"`
- Lines 120-121: `"readable_text"` → `"constraints:readable_text"`
- Line 129: `"local_storefront"` → `"subject:local_storefront"`

**Verification sequence after all updates:**
1. `npx vitest run` — full suite must pass
2. `npx tsc --noEmit` — no type errors
3. `npx vitest run src/lib/prompt/validation.test.ts -t "safety"` — safetyDefaults integrity passes
4. `npx vitest run src/lib/prompt/validation.test.ts -t "unique"` — no duplicate IDs
5. `npx vitest run src/lib/prompt/validation.test.ts -t "validateOptionIdFormat"` — empty errors

**DO NOT touch:** templateMap keys in target files. Verified by grep — all templateMap keys are question dimension IDs (use_case, subject, scene, etc.), not option IDs.
  </action>

  <verify>
    <automated>npx vitest run</automated>
  </verify>

  <done>
Full test suite passes (58 tests). TypeScript compiles. validateOptionIdFormat() returns [] for all catalogs. validateSafetyDefaultsIntegrity() returns [] for all targets. validateOptionIdsUnique() returns []. All option ID references in defaults, safetyDefaults, and test fixtures use namespace prefix format. templateMap keys untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog data → Registration | Option IDs enter the registry at import time |
| Target config → Renderer | safetyDefaults option IDs resolved during rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `registerOptionSet()` | mitigate | `validateOptionIdsUnique()` CI check catches duplicate IDs across catalogs (already exists) |
| T-04-02 | Information Disclosure | `renderPrompt()` | mitigate | `validateSafetyDefaultsIntegrity()` CI check catches broken safetyDefaults references (already exists) |
| T-04-03 | Tampering | Option catalog files | mitigate | `validateOptionIdFormat()` added in this plan — CI check ensures every option ID has namespace prefix, preventing bare-ID collision across catalogs |
</threat_model>

<verification>
## Phase-level verification
- `npx vitest run` — full test suite (58 tests) green
- `npx tsc --noEmit` — zero type errors
- `npx eslint .` — zero new lint violations
- `npx next build` — static export succeeds
</verification>

<success_criteria>
- [ ] `validateOptionIdFormat()` function exists in validation.ts with 4 unit tests
- [ ] All 136 option IDs in 12 catalog files use `catalogId:optionId` format
- [ ] All 7 reference locations updated (prompt-guide.tsx, 3 target files, validation.test.ts, reducer.test.ts)
- [ ] templateMap keys in target files are untouched (verified by git diff)
- [ ] Full test suite passes: `npx vitest run` — 58 tests green
- [ ] CI validations all pass: uniqueness, format, safety integrity, target refs
- [ ] TypeScript compilation succeeds: `npx tsc --noEmit`
- [ ] Static export succeeds: `npm run build`
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalog-expansion/04-01-SUMMARY.md`
</output>
