---
phase: 04-catalog-expansion
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/lib/prompt/options/constraints.options.ts
  - src/lib/prompt/options/text-handling.options.ts
  - src/lib/prompt/options/format.options.ts
  - src/lib/prompt/options/shot-type.options.ts
  - src/lib/prompt/options/camera-movement.options.ts
  - src/lib/prompt/options/subject.options.ts
  - src/lib/prompt/options/motion.options.ts
  - src/lib/prompt/options/audio.options.ts
  - src/lib/prompt/options/use-case.options.ts
  - src/lib/prompt/options/scene.options.ts
  - src/lib/prompt/options/style.options.ts
  - src/lib/prompt/options/lighting.options.ts
  - src/lib/prompt/validation.test.ts
autonomous: true
requirements:
  - OPT-02

must_haves:
  truths:
    - "Every option across all 12 catalogs has a riskHint field (empty or substantive)"
    - "All constraints options have substantive (non-empty zh+en) riskHint"
    - "All text_handling options have substantive (non-empty zh+en) riskHint"
    - "LOW-risk catalogs (lighting, style, scene, audio, use-case) have riskHint present but may be empty"
    - "Borderline catalogs (format, shot-type, camera-movement, subject, motion) have riskHint per Claude's discretion"
  artifacts:
    - path: "src/lib/prompt/options/constraints.options.ts"
      provides: "All 15 constraints options with substantive riskHint"
      contains: "riskHint"
    - path: "src/lib/prompt/options/text-handling.options.ts"
      provides: "All 15 text-handling options with substantive riskHint"
      contains: "riskHint"
    - path: "src/lib/prompt/validation.test.ts"
      provides: "CI test verifying every option has riskHint field"
      contains: "riskHint"
  key_links:
    - from: "riskHint field on OptionItem"
      to: "QuestionBlock UI rendering"
      via: "option.riskHint?.zh displayed as amber warning"
      pattern: "option.riskHint ? <p className=\"...amber-700\">"
    - from: "validation.test.ts riskHint check"
      to: "all option catalogs"
      via: "getAllOptionSets() iteration"
      pattern: "every option has riskHint"
---

<objective>
Complete the riskHint metadata field for every option across all 12 catalogs. Constraints and text_handling get substantive warnings. LOW-risk catalogs get empty strings. Borderline catalogs get warnings at Claude's discretion. Add a CI test to enforce completeness.

Purpose: Fulfill D-03 — every option must have riskHint populated. Currently only 17/136 options have riskHint. This is the final metadata quality pass.

Output: All ~186 options have riskHint field. 30+ substantive riskHints. 1 new CI validation test. All tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-catalog-expansion/04-CONTEXT.md (D-03 locked decision)
@.planning/phases/04-catalog-expansion/04-RESEARCH.md (Risk Classification Framework section)

## D-03 locked requirements

**Locked:** riskHint must be substantive for constraints and text_handling catalogs (where risk is real).
**Locked:** May be empty for lighting, style, scene, audio, use-case (where risk is low or non-existent).
**Claude's Discretion:** riskHint content for borderline catalogs (format, shot-type, camera-movement, subject, motion).

## Risk classification matrix (from RESEARCH.md)

| Catalog | Risk Level | riskHint Required | Approach |
|---------|-----------|-------------------|----------|
| constraints | **HIGH** | Substantive (zh+en) | Model limitations + specific mitigation advice |
| text_handling | **HIGH** | Substantive (zh+en) | Text accuracy warning + post-production recommendation |
| format | BORDERLINE | Discretion | Platform compatibility caveats where relevant |
| shot-type | BORDERLINE | Discretion | Aesthetic risk for extreme angles |
| camera-movement | BORDERLINE | Discretion | Motion artifact risk for complex movements |
| subject | BORDERLINE | Discretion | IP/privacy risk for people/vehicles/brands |
| motion | BORDERLINE | Discretion | Motion precision limitations |
| audio | LOW | Empty string "" | N/A |
| use-case | LOW | Empty string "" | N/A |
| scene | LOW | Empty string "" | N/A |
| style | LOW | Empty string "" | N/A |
| lighting | LOW | Empty string "" | N/A |

## Existing riskHint patterns (from codebase — verified)

**HIGH risk (constraints, text_handling):** Pattern is `"{limitation}. {recommendation}"`

```typescript
// Existing examples from constraints.options.ts:
riskHint: { zh: "多数视频模型对画面文字仍不稳定，重要文字建议后期添加。", en: "Most video models still render text unreliably; add critical text in post-production." }
riskHint: { zh: "真实品牌、价格和活动文案最好后期添加。", en: "Real brand names, prices, and campaign copy are safer to add in post." }

// Existing examples from text-handling.options.ts:
riskHint: { zh: "AI 生成文字可能出现错字，重要文案建议后期替换。", en: "AI-generated text may contain errors; replace critical copy in post." }
riskHint: { zh: "AI 生成数字和文字可能不准确，关键价格请后期确认。", en: "AI-generated numbers and text may be inaccurate; verify key prices in post." }
```

**Borderline risk (subject, shot-type):**

```typescript
// Existing example from subject.options.ts:
riskHint: { zh: "不要指定明星、网红或真实未授权人物。", en: "Do not specify celebrities, influencers, or unauthorized real people." }
```

## Existing riskHint inventory (which options already have one)

**Already have riskHint (17 options):**
- constraints: readable_text, no_logo_hallucination
- text_handling: short_title_only, opening_title_card, price_tag_popup, end_card_cta
- style: 4 options
- subject: 3 options
- scene: 2 options
- motion: 1 option
- shot-type: 1 option

**Need riskHint (119+ existing + ~50 new):**
All remaining options. New options from Plan 04-02 were created with empty riskHint strings.

## Open question from RESEARCH.md (Q1): Keep or remove existing riskHint in LOW-risk catalogs?

**Recommendation from RESEARCH.md:** Keep existing riskHint on options where the warning is genuinely useful (e.g., scene/beach_poolside warns about bystander privacy). Remove only if vague or unnecessary. **Decision for this plan:** KEEP all existing riskHint values. Only add new ones or set to empty strings on options that have none.

## Existing code interfaces

From src/lib/prompt/types.ts:
```typescript
export interface OptionItem {
  ...
  riskHint?: LocalizedText;  // NOTE: optional (?), but D-03 requires it on all options
  ...
}
```

From src/components/prompt-guide.tsx (riskHint rendering, line 186):
```tsx
{option.riskHint ? <p className="mt-3 text-xs leading-5 text-amber-700">{option.riskHint.zh}</p> : null}
```

From src/lib/prompt/validation.ts (existing functions):
```typescript
export function validateOptionIdsUnique(optionSets_?: OptionSet[]): string[];
export function getAllOptionSets(): OptionSet[];
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add riskHint completeness CI test to validation.test.ts</name>
  <files>src/lib/prompt/validation.test.ts</files>

  <read_first>
@src/lib/prompt/validation.test.ts
@src/lib/prompt/validation.ts
@src/lib/prompt/types.ts (OptionItem.riskHint field definition)
  </read_first>

  <behavior>
    - Test 1: Every option in getAllOptionSets() has riskHint property that is not undefined
    - Test 2: All constraints options have non-empty riskHint.zh AND riskHint.en
    - Test 3: All text_handling options have non-empty riskHint.zh AND riskHint.en
    - Test 4: Options in LOW-risk catalogs (lighting, style, scene, audio, use_case) may have empty riskHint strings but must have the field present
  </behavior>

  <action>
Add a new `describe("riskHint completeness (D-03)")` block to validation.test.ts.

The test suite must:

1. Iterate all option sets via `getAllOptionSets()` (import from registry, not from options barrel — the barrel does its own auto-registration at import time).

2. **Test: every option has riskHint field** — Check that `option.riskHint !== undefined` for every option. Collect any option IDs missing the field. Fail with clear message listing them.

3. **Test: HIGH-risk catalogs have substantive riskHint** — For `constraints` and `text_handling` catalogs, verify that every option has `riskHint.zh.length > 0 && riskHint.en.length > 0`. Collect violations and fail with option IDs.

4. **Test: LOW-risk catalogs have riskHint present** — For `lighting`, `style`, `scene`, `audio`, `use_case` catalogs, verify that `riskHint` field exists (may be empty strings). No need to check content length.

5. **Test: borderline catalogs have riskHint present** — For `format`, `shot_type`, `camera_movement`, `subject`, `motion`, verify that `riskHint` field exists (content may be empty or substantive per discretion).

Note: The registry auto-registers all option sets at import time (via `@/lib/prompt/init` or `options/index.ts` barrel). Use `getAllOptionSets()` from `@/lib/prompt/registry`.
  </action>

  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"</automated>
  </verify>

  <done>
CI riskHint test exists in validation.test.ts with 4 sub-tests: all options have field, HIGH-risk catalogs have substantive content, LOW-risk catalogs have field present, borderline catalogs have field present. Tests initially FAIL because many options lack riskHint — green only after Tasks 2-3 complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add substantive riskHint to all HIGH-risk catalog options (constraints + text_handling)</name>
  <files>
    src/lib/prompt/options/constraints.options.ts,
    src/lib/prompt/options/text-handling.options.ts
  </files>

  <read_first>
@src/lib/prompt/options/constraints.options.ts
@src/lib/prompt/options/text-handling.options.ts
@src/lib/prompt/validation.test.ts (Task 1 riskHint test — read to understand expected format)
  </read_first>

  <acceptance_criteria>
    - Every option in constraints.options.ts has riskHint with non-empty zh AND en strings
    - Every option in text-handling.options.ts has riskHint with non-empty zh AND en strings
    - Existing riskHint values on constraints/readable_text, constraints/no_logo_hallucination, and all text_handling options are preserved
    - CI riskHint test (Task 1) passes for HIGH-risk catalogs
    - `npx tsc --noEmit` passes
    - Pattern: `"{limitation}. {recommendation}"` consistent with existing examples
  </acceptance_criteria>

  <action>
**per D-03**: Add substantive riskHint to every constraints and text_handling option that doesn't already have one.

### constraints.options.ts (10 existing + 5 new from Plan 04-02)

**Already have riskHint**: readable_text, no_logo_hallucination — KEEP existing values.

**Add riskHint to these 8 existing constraints** (use namespace-prefixed IDs as reference):

- `constraints:no_ip_or_celebrity`: { zh: "部分模型可能仍生成类似明星或知名 IP 的形象。如有品牌安全需求，建议人工复核输出。", en: "Some models may still generate likenesses resembling celebrities or known IP. If brand safety is critical, manually review outputs." }

- `constraints:stable_identity`: { zh: "视频模型在长镜头中可能发生主体漂移（换脸、变形）。保持镜头时长在模型稳定范围内可降低风险。", en: "Video models may experience subject drift (face swaps, distortion) in long shots. Keep shot duration within the model's stable range to reduce risk." }

- `constraints:simple_scene`: { zh: "场景越复杂，模型出错概率越高。简洁场景可显著提升生成成功率和一致性。", en: "More complex scenes have higher failure rates. Simple scenes significantly improve generation success rate and consistency." }

- `constraints:no_extra_limbs`: { zh: "视频模型对手部和肢体的生成仍不稳定，多手指、多肢体和扭曲关节是已知常见问题。", en: "Video models still struggle with hand and limb generation; extra fingers, extra limbs, and twisted joints are well-known common issues." }

- `constraints:single_focal_subject`: { zh: "多主体场景容易导致模型注意力分散，主体可能变形或被忽略。明确单一视觉重点可提升可控性。", en: "Multiple subjects often cause the model to lose focus, leading to deformation or neglect of the intended subject. A single clear focal point improves controllability." }

- `constraints:avoid_temporal_flicker`: { zh: "Seedance 对帧间一致性敏感。闪烁和跳变在某些复杂场景下仍可能出现，建议生成后逐帧检查。", en: "Seedance is sensitive to frame-to-frame consistency. Flicker and jitter may still occur in complex scenes; review frame-by-frame after generation." }

- `constraints:avoid_quality_keywords`: { zh: "Seedance 2.0 对特定关键词（fast, cinematic, epic, amazing 等）敏感，这些词会降低输出质量。避免在 prompt 中使用它们。", en: "Seedance 2.0 is sensitive to specific keywords (fast, cinematic, epic, amazing, etc.) that degrade output quality. Avoid them in prompts." }

**Add riskHint to the 5 new constraints** (added in Plan 04-02, currently have empty riskHint):

- `constraints:no_rapid_cuts`: { zh: "快速跳切可能导致帧间不连贯。生成后建议检查镜头过渡的流畅度。", en: "Rapid cuts may cause inter-frame discontinuity. Review shot transition smoothness after generation." }

- `constraints:avoid_water_reflections`: { zh: "视频模型对反射和透明表面的处理仍不稳定，水面和玻璃内容可能出现高差错。若场景必须包含这些元素，建议减少镜头停留时间。", en: "Video models still struggle with reflections and transparent surfaces; water and glass content may have high error rates. If these elements are essential, minimize camera dwell time." }

- `constraints:avoid_glass_artifacts`: { zh: "透明物体（玻璃、亚克力）在视频模型中容易出现穿模和折射错误。关键画面建议减少透明元素或降低复杂度。", en: "Transparent objects (glass, acrylic) often exhibit clipping and refraction errors in video models. Reduce transparent elements or simplify for critical shots." }

- `constraints:background_consistency`: { zh: "镜头切换时背景可能发生突变或元素位移，尤其在有复杂道具的场景中。建议生成后检查镜头间的背景连续性。", en: "Backgrounds may shift or lose elements between cuts, especially in scenes with complex props. Check background continuity across cuts after generation." }

- `constraints:limit_motion_amplitude`: { zh: "大幅度快速动作可能导致画面撕裂、畸变或主体模糊。如需动态场景，建议降低动作速度或增加镜头稳定性提示。", en: "Large, fast motions may cause frame tearing, distortion, or subject blur. For dynamic scenes, reduce motion speed or add camera stability cues." }

### text-handling.options.ts (8 existing + 7 new from Plan 04-02)

**Already have riskHint**: short_title_only, opening_title_card, price_tag_popup, end_card_cta — KEEP existing values.

**Add riskHint to these 4 existing text_handling options**:

- `text_handling:no_text`: { zh: "部分模型可能在纯画面场景中仍生成意外文字。生成后检查画面是否完全干净，可考虑后期裁剪意外文字区域。", en: "Some models may still generate unexpected text in text-free scenes. Check if the frame is fully clean after generation; consider cropping accidental text areas in post." }

- `text_handling:placeholder_signage`: { zh: "模糊招牌可能被模型解读为可识别文字。如需完全避免文字，选择'无文字/纯画面'选项更稳妥。", en: "Blurred signage may be interpreted as readable text by the model. For complete text avoidance, the 'No text, pure visual' option is more reliable." }

- `text_handling:bottom_subtitle_bar`: { zh: "模型可能忽略或错位字幕条区域。生成后确认底部区域是否按预期留空。", en: "Models may ignore or misplace the subtitle bar area. Verify the bottom zone is correctly reserved after generation." }

- `text_handling:no_text_pure_visual`: { zh: "视频模型有时仍会在画面中生成意料之外的文字。如出现，建议后期裁切或遮罩处理。", en: "Video models may still hallucinate unexpected text in the frame. If it occurs, crop or mask in post-production." }

**Add riskHint to the 7 new text_handling options** (added in Plan 04-02, currently have empty riskHint):

- `text_handling:animated_text_reveal`: { zh: "AI 生成的逐字动画可能出现文字错位或抖动。重要文案建议后期用专业工具实现动画效果。", en: "AI-generated text reveal animations may have misalignment or jitter. For critical copy, implement animation in professional post-production tools." }

- `text_handling:scrolling_credits`: { zh: "AI 生成的滚动字幕可能出现文字模糊、变形或速度不均。建议后期用剪辑软件添加专业字幕。", en: "AI-generated scrolling credits may appear blurry, distorted, or uneven in speed. Add credits in professional editing software for best results." }

- `text_handling:chapter_markers`: { zh: "AI 生成的标题卡文字可能不准确或排版混乱。章节标记建议后期替换为设计稿。", en: "AI-generated chapter title text may be inaccurate or poorly laid out. Replace chapter markers with designed graphics in post." }

- `text_handling:watermark_corner`: { zh: "AI 生成的水印可能模糊或位置偏移。品牌水印建议后期添加以保证清晰度和定位准确。", en: "AI-generated watermarks may be blurry or mispositioned. Add brand watermarks in post for guaranteed clarity and placement." }

- `text_handling:text_on_object`: { zh: "文字跟随移动物体的追踪精度有限，可能出现漂移或错位。关键标注建议后期合成。", en: "Text tracking on moving objects has limited precision and may drift or misalign. Composite critical labels in post-production." }

- `text_handling:bilingual_subtitles`: { zh: "AI 生成的 bilingual 字幕可能有错译、语法错误或排版重叠。正式发布前建议人工校对双语内容。", en: "AI-generated bilingual subtitles may contain mistranslations, grammar errors, or layout overlaps. Manually proofread bilingual content before publishing." }

- `text_handling:lower_third_overlay`: { zh: "AI 生成的底部信息条文字可能出现错字、排版错位。真实人物姓名和职位建议后期确认替换。", en: "AI-generated lower third text may have typos or layout misalignment. Verify and replace real names and titles in post." }

**Verify after all updates:**
- Every option in constraints.options.ts has `riskHint.zh.length > 0 && riskHint.en.length > 0`
- Every option in text-handling.options.ts has `riskHint.zh.length > 0 && riskHint.en.length > 0`
- All existing riskHint values preserved (not overwritten)
- `npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"` — HIGH-risk tests pass
- `npx tsc --noEmit` — compiles
  </action>

  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"</automated>
  </verify>

  <done>
All 15 constraints options have substantive riskHint. All 15 text_handling options have substantive riskHint. Existing riskHint values preserved. CI riskHint test confirms HIGH-risk catalogs have non-empty zh+en content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add riskHint to borderline catalogs and ensure empty riskHint on LOW-risk catalogs</name>
  <files>
    src/lib/prompt/options/format.options.ts,
    src/lib/prompt/options/shot-type.options.ts,
    src/lib/prompt/options/camera-movement.options.ts,
    src/lib/prompt/options/subject.options.ts,
    src/lib/prompt/options/motion.options.ts,
    src/lib/prompt/options/audio.options.ts,
    src/lib/prompt/options/use-case.options.ts,
    src/lib/prompt/options/scene.options.ts,
    src/lib/prompt/options/style.options.ts,
    src/lib/prompt/options/lighting.options.ts
  </files>

  <read_first>
@src/lib/prompt/options/format.options.ts
@src/lib/prompt/options/shot-type.options.ts
@src/lib/prompt/options/camera-movement.options.ts
@src/lib/prompt/options/subject.options.ts
@src/lib/prompt/options/motion.options.ts
@src/lib/prompt/options/audio.options.ts
@src/lib/prompt/options/use-case.options.ts
@src/lib/prompt/options/scene.options.ts
@src/lib/prompt/options/style.options.ts
@src/lib/prompt/options/lighting.options.ts
@src/lib/prompt/validation.test.ts (Task 1 riskHint test — understand per-catalog expectations)
  </read_first>

  <acceptance_criteria>
    - Every option in borderline catalogs (format, shot-type, camera-movement, subject, motion) has riskHint field present
    - New borderline options (from Plan 04-02) get riskHint at Claude's discretion — substantive for real risks, empty for safe options
    - Every option in LOW-risk catalogs (audio, use-case, scene, style, lighting) has riskHint field present
    - Existing riskHint values in style (4), subject (3), scene (2), motion (1), shot-type (1) are PRESERVED
    - LOW-risk catalog options without existing riskHint get `riskHint: { zh: "", en: "" }`
    - CI riskHint test (Task 1) fully passes — all catalogs, all tests
    - `npx vitest run` — full test suite passes
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <action>
**per D-03**: Add riskHint to borderline catalogs and set empty riskHint on LOW-risk catalogs.

### Approach

1. **First, read every file** to see which options already have riskHint and which don't.
2. **Preserve ALL existing riskHint values** — do not remove or modify any existing riskHint.
3. **For existing options without riskHint**: Add `riskHint: { zh: "", en: "" }` (LOW-risk) or add a substantive warning (borderline where real risk exists).
4. **For new options from Plan 04-02**: Add `riskHint: { zh: "", en: "" }` (they currently have this — verify it's present).

### Borderline catalog guidelines

**format (BORDERLINE)**: Most format options are safe — no risk. Set empty riskHint for most. Add substantive riskHint ONLY for extreme/niche formats:
- `format:wide_219_15s` (21:9 ultrawide): { zh: "超宽画幅在部分视频模型中可能出现画面裁切异常或构图偏移。生成后确认画面完整性。", en: "Ultrawide formats may cause unexpected cropping or composition shifts in some video models. Verify frame integrity after generation." }
- All other format options (existing + new): `riskHint: { zh: "", en: "" }`

**shot-type (BORDERLINE)**: Most are safe. Add substantive riskHint only for extreme angles:
- `shot_type:low_angle` (new, Plan 04-02): { zh: "极端低角度可能导致主体比例失真或背景透视异常。", en: "Extreme low angles may cause subject proportion distortion or background perspective anomalies." }
- `shot_type:high_angle` (new, Plan 04-02): { zh: "高角度俯拍可能导致场景扁平化，失去空间深度感。", en: "High angle shots may flatten the scene, losing spatial depth perception." }
- `shot_type:split_screen` (new, Plan 04-02): { zh: "分屏合成可能因模型对画面边界的理解不准确而出现错位。", en: "Split-screen compositing may misalign due to the model's imperfect understanding of frame boundaries." }
- All other shot-type options (existing + new over-shoulder, two-shot, insert_detail): `riskHint: { zh: "", en: "" }`
- **Preserve** existing riskHint on shot-type options that already have one.

**camera-movement (BORDERLINE)**: Complex movements have real artifact risk:
- `camera_movement:whip_pan` (new, Plan 04-02): { zh: "快速甩镜在视频模型中容易产生画面撕裂和运动模糊伪影。建议控制甩镜速度。", en: "Fast whip pans in video models often produce frame tearing and motion blur artifacts. Control pan speed for best results." }
- `camera_movement:rack_focus` (new, Plan 04-02): { zh: "移焦效果可能在视频模型中不够平滑，焦点转移可能出现跳变。", en: "Rack focus effects may not be smooth in video models; focus transitions may jump." }
- All other camera-movement options (existing + new zoom_in, zoom_out, crane_up, steadicam_float): `riskHint: { zh: "", en: "" }`

**subject (BORDERLINE)**: People-related subjects have privacy/IP risk:
- `subject:baby_children` (new, Plan 04-02): { zh: "儿童相关画面需特别注意隐私和肖像权，避免使用可识别的真实儿童形象。", en: "Children-related content requires special attention to privacy and likeness rights; avoid recognizable real children." }
- `subject:cosmetics_beauty` (new, Plan 04-02): empty riskHint (product-focused, low risk)
- **Preserve** existing riskHint on subject options that already have one (3 total).
- All other subject options without riskHint: `riskHint: { zh: "", en: "" }`

**motion (BORDERLINE)**: Only the complex motion has risk:
- `motion:timelapse_hyperlapse` (new, Plan 04-02): { zh: "延时效果在视频模型中可能出现帧间不一致，导致画面闪烁或跳帧。建议生成后检查流畅度。", en: "Time-lapse effects may have frame-to-frame inconsistency in video models, causing flicker or skipped frames. Check smoothness after generation." }
- **Preserve** existing riskHint on motion options that already have one (1 total).
- All other motion options without riskHint: `riskHint: { zh: "", en: "" }`

### LOW-risk catalog guidelines (all get empty riskHint)

For **audio** (13 existing + 2 new), **use-case** (12 existing + 3 new), **scene** (13 existing + 2 new), **style** (15 existing), **lighting** (14 existing + 1 new):
- **Preserve** ALL existing riskHint values (style has 4, scene has 2).
- For every option that does NOT already have a riskHint: add `riskHint: { zh: "", en: "" }`.
- New options from Plan 04-02 already have empty riskHint — verify and ensure consistency.

### Execution order (process file by file)

For each of the 10 catalog files:
1. Read the file
2. Identify options missing `riskHint` field entirely — add `riskHint: { zh: "", en: "" }`
3. Identify new options from Plan 04-02 that need substantive riskHint per guidelines above — replace empty with substantive
4. Identify borderline options needing substantive riskHint per guidelines above — add
5. Verify ALL existing riskHint values are preserved (git diff should show only additions, no modifications to existing riskHint)

### Verification
- `npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"` — all 4 riskHint sub-tests pass
- `npx vitest run` — full suite passes
- `npx tsc --noEmit` — compiles
- Git diff confirms: only additions of riskHint, no removal or modification of existing riskHint values
  </action>

  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"</automated>
  </verify>

  <done>
All options across 12 catalogs have riskHint field. HIGH-risk catalogs (constraints, text_handling) have substantive zh+en content. Borderline catalogs have riskHint per discretion guidelines. LOW-risk catalogs have riskHint field present (may be empty). CI riskHint test fully passes. Full test suite passes. All existing riskHint values preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| riskHint data → UI rendering | riskHint.zh displayed as amber warning text in QuestionBlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Missing riskHint on HIGH-risk options | mitigate | CI test in Task 1 enforces non-empty riskHint for constraints and text_handling catalogs |
| T-04-08 | Spoofing | Misleading riskHint content | accept | riskHint is advisory text displayed as amber warnings — no authentication or authorization impact. Content quality is Claude's responsibility during execution. |
</threat_model>

<verification>
## Phase-level verification
- `npx vitest run src/lib/prompt/validation.test.ts -t "riskHint"` — all 4 sub-tests pass
- `npx vitest run` — full test suite green
- `npx tsc --noEmit` — zero type errors
- Manual: spot-check 3 random options from constraints, text_handling, and a LOW catalog — verify riskHint exists and content matches guidelines
</verification>

<success_criteria>
- [ ] CI test verifies every option in every catalog has riskHint field (no undefineds)
- [ ] CI test verifies all constraints options have non-empty zh+en riskHint
- [ ] CI test verifies all text_handling options have non-empty zh+en riskHint
- [ ] CI test verifies all LOW-risk catalog options have riskHint field present
- [ ] All ~186 options have riskHint populated (0 missing, 0 undefined)
- [ ] constraints: 15/15 substantive riskHints
- [ ] text_handling: 15/15 substantive riskHints
- [ ] Existing riskHint values on 17 options preserved (git diff confirms no modifications)
- [ ] Full test suite passes
- [ ] TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalog-expansion/04-03-SUMMARY.md`
</output>
