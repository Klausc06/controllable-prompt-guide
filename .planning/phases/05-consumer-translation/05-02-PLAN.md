---
phase: 05-consumer-translation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/prompt/options/style.options.ts
  - src/lib/prompt/options/subject.options.ts
  - src/lib/prompt/options/use-case.options.ts
  - src/lib/prompt/options/format.options.ts
  - src/lib/prompt/platforms/platform-data.ts
autonomous: true
requirements: [DIFF-04, DIFF-05]

must_haves:
  truths:
    - "Style option set has 4 categories: 写实/真实感, 氛围/质感, 精致/商业, 艺术/特殊风格 covering all 18 options"
    - "Subject option set has 4 categories: 人物, 产品, 空间/场所, 其他主体 covering all 15 options"
    - "Use Case option set has 3 categories: 商业推广, 社交媒体, 品牌/活动 covering all 15 options"
    - "Format option set has 4 categories: 竖屏 9:16, 横屏 16:9, 方形 1:1, 其他比例 covering all 15 options"
    - "Platform config file exists with 4 domestic platforms (抖音, 小红书, B站, 视频号) each recommending 2-3 format option IDs"
    - "PlatformConfig type has id, label: LocalizedText, recommendedFormats: string[] fields"
  artifacts:
    - path: "src/lib/prompt/options/style.options.ts"
      provides: "Style OptionSet with categories array (4 categories covering 18 options)"
      contains: "categories:"
    - path: "src/lib/prompt/options/subject.options.ts"
      provides: "Subject OptionSet with categories array (4 categories covering 15 options)"
      contains: "categories:"
    - path: "src/lib/prompt/options/use-case.options.ts"
      provides: "Use Case OptionSet with categories array (3 categories covering 15 options)"
      contains: "categories:"
    - path: "src/lib/prompt/options/format.options.ts"
      provides: "Format OptionSet with categories array (4 categories covering 15 options)"
      contains: "categories:"
    - path: "src/lib/prompt/platforms/platform-data.ts"
      provides: "PlatformConfig type and platformConfigs array (4 entries)"
      exports: ["PlatformConfig", "platformConfigs"]
  key_links:
    - from: "categories.optionIds"
      to: "OptionItem.id in the same OptionSet"
      via: "string array referencing option IDs"
      pattern: "optionIds:"
    - from: "platformConfigs[].recommendedFormats"
      to: "format options in format.options.ts"
      via: "string array referencing format option IDs"
      pattern: "format:vertical_10s"
---

<objective>
Add category definitions to all four large option sets (Style, Subject, Use Case, Format) per D-07 (sets with >= 12 options), and create the platform config file mapping 4 domestic platforms to their recommended format option IDs.

Purpose: Enable category tab filtering (DIFF-04) and platform-specific format shortcuts (DIFF-05). Categories group options within large sets for easier navigation. Platform config provides the data that platform quick-entry tags use to select recommended format options.

Output: 4 option files enriched with categories arrays, and 1 new platform config file at `src/lib/prompt/platforms/platform-data.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-consumer-translation/05-CONTEXT.md
@.planning/phases/05-consumer-translation/05-RESEARCH.md
@.planning/phases/05-consumer-translation/05-UI-SPEC.md
@.planning/phases/05-consumer-translation/05-01-SUMMARY.md
@src/lib/prompt/types.ts
@src/lib/prompt/options/style.options.ts
@src/lib/prompt/options/subject.options.ts
@src/lib/prompt/options/use-case.options.ts
@src/lib/prompt/options/format.options.ts

<interfaces>
From src/lib/prompt/types.ts (after Plan 05-01):
```typescript
export interface OptionSet {
  id: string;
  version: string;
  label: LocalizedText;
  options: OptionItem[];
  categories?: { id: string; label: LocalizedText; optionIds: string[] }[];
}

export interface LocalizedText {
  zh: string;
  en: string;
}
```

Style option IDs (18 total after Plan 05-01, including 3 new ones):
- style:cinematic_realism, style:ugc_handheld, style:premium_minimal, style:cyberpunk_neon
- style:warm_lifestyle, style:luxury_editorial, style:clean_tech, style:documentary_real
- style:fresh_natural_forest, style:dark_moody_textured, style:retro_vhs_ccd, style:cg_3d_rendered
- style:black_white_monochrome, style:pop_art_collage, style:anime_manga_inspired
- style:healing_comfort, style:trend_street_editorial, style:dreamy_ethereal (NEW from Plan 05-01)

Subject option IDs (15): subject:local_storefront, subject:hero_product, subject:human_customer, subject:staff_expert, subject:space_environment, subject:food_drink, subject:person_model, subject:vehicles_transportation, subject:electronics_gadgets, subject:jewelry_accessories, subject:architecture_interior, subject:pets_animals, subject:cosmetics_beauty, subject:baby_children, subject:sports_fitness_gear

Use Case option IDs (15): use_case:gym_opening, use_case:coffee_new_product, use_case:city_travel_promo, use_case:product_showcase, use_case:xiaohongshu_visit, use_case:course_promo, use_case:product_unboxing, use_case:event_recap, use_case:tutorial_demo, use_case:brand_image, use_case:social_media_quick_clip, use_case:livestream_clip, use_case:real_estate_tour, use_case:corporate_event, use_case:restaurant_menu_showcase

Format option IDs (15): format:vertical_3s, format:vertical_5s, format:vertical_10s, format:vertical_15s, format:vertical_30s, format:vertical_60s, format:horizontal_12s, format:horizontal_30s, format:horizontal_60s, format:square_8s, format:square_15s, format:square_30s, format:portrait_34_8s, format:portrait_45_10s, format:wide_219_15s
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add categories to all four large option sets (Style, Subject, Use Case, Format)</name>
  <files>
    src/lib/prompt/options/style.options.ts
    src/lib/prompt/options/subject.options.ts
    src/lib/prompt/options/use-case.options.ts
    src/lib/prompt/options/format.options.ts
  </files>
  <read_first>
- src/lib/prompt/options/style.options.ts (post-Plan-05-01 version with 18 options + consumerTerms)
- src/lib/prompt/options/subject.options.ts (15 options, lines 1-205)
- src/lib/prompt/options/use-case.options.ts (15 options, lines 1-193)
- src/lib/prompt/options/format.options.ts (15 options, lines 1-160)
  </read_first>
  <acceptance_criteria>
- style.options.ts contains `categories:` array with exactly 4 category entries (grep: `"cat:style:"` appears exactly 4 times)
- subject.options.ts contains `categories:` array with exactly 4 category entries (grep: `"cat:subject:"` appears exactly 4 times)
- use-case.options.ts contains `categories:` array with exactly 3 category entries (grep: `"cat:use_case:"` appears exactly 3 times)
- format.options.ts contains `categories:` array with exactly 4 category entries (grep: `"cat:format:"` appears exactly 4 times)
- All 18 style option IDs appear in at least one style category optionIds
- All 15 subject option IDs appear in at least one subject category optionIds
- All 15 use case option IDs appear in at least one use case category optionIds
- All 15 format option IDs appear in at least one format category optionIds
- Category IDs use the `cat:{setId}:{slug}` naming convention
- All category labels are bilingual (zh and en)
- Consumer term mappings from Plan 05-01 remain intact (grep `consumerTerms:` in style.options.ts still shows >= 12 matches)
- `npx tsc --noEmit` passes
- `npm test` passes
  </acceptance_criteria>
  <action>

Add a `categories` field to each of the four OptionSet object literals. Insert the field after the closing `]` of the `options` array but before the closing `};` of the OptionSet object.

**File 1: `src/lib/prompt/options/style.options.ts`**

The file now has 18 options (15 original + 3 new from Plan 05-01). Add after the `options` array closing `]`:

```typescript
  categories: [
    {
      id: "cat:style:realistic",
      label: { zh: "写实/真实感", en: "Realistic" },
      optionIds: ["style:cinematic_realism", "style:ugc_handheld", "style:warm_lifestyle", "style:documentary_real"]
    },
    {
      id: "cat:style:mood",
      label: { zh: "氛围/质感", en: "Mood & Texture" },
      optionIds: ["style:dark_moody_textured", "style:healing_comfort", "style:dreamy_ethereal", "style:fresh_natural_forest"]
    },
    {
      id: "cat:style:editorial",
      label: { zh: "精致/商业", en: "Editorial & Commercial" },
      optionIds: ["style:premium_minimal", "style:luxury_editorial", "style:clean_tech", "style:trend_street_editorial"]
    },
    {
      id: "cat:style:artistic",
      label: { zh: "艺术/特殊风格", en: "Artistic & Special" },
      optionIds: ["style:cyberpunk_neon", "style:retro_vhs_ccd", "style:cg_3d_rendered", "style:black_white_monochrome", "style:pop_art_collage", "style:anime_manga_inspired"]
    }
  ]
```

Coverage: 4 + 4 + 4 + 6 = 18. All 18 style options covered.

**File 2: `src/lib/prompt/options/subject.options.ts`**

Add after the `options` array closing `]` (line ~203):

```typescript
  categories: [
    {
      id: "cat:subject:people",
      label: { zh: "人物", en: "People" },
      optionIds: ["subject:human_customer", "subject:staff_expert", "subject:person_model"]
    },
    {
      id: "cat:subject:product",
      label: { zh: "产品", en: "Products" },
      optionIds: [
        "subject:hero_product",
        "subject:electronics_gadgets",
        "subject:jewelry_accessories",
        "subject:cosmetics_beauty",
        "subject:sports_fitness_gear"
      ]
    },
    {
      id: "cat:subject:space",
      label: { zh: "空间/场所", en: "Spaces" },
      optionIds: ["subject:local_storefront", "subject:space_environment", "subject:architecture_interior"]
    },
    {
      id: "cat:subject:other",
      label: { zh: "其他主体", en: "Other" },
      optionIds: ["subject:food_drink", "subject:vehicles_transportation", "subject:pets_animals", "subject:baby_children"]
    }
  ]
```

Coverage: 3 + 5 + 3 + 4 = 15. All 15 subject options covered.

**File 3: `src/lib/prompt/options/use-case.options.ts`**

Add after the `options` array closing `]` (line ~192):

```typescript
  categories: [
    {
      id: "cat:use_case:biz",
      label: { zh: "商业推广", en: "Business Promo" },
      optionIds: [
        "use_case:gym_opening",
        "use_case:coffee_new_product",
        "use_case:product_showcase",
        "use_case:product_unboxing",
        "use_case:restaurant_menu_showcase",
        "use_case:course_promo",
        "use_case:real_estate_tour"
      ]
    },
    {
      id: "cat:use_case:social",
      label: { zh: "社交媒体", en: "Social Media" },
      optionIds: [
        "use_case:xiaohongshu_visit",
        "use_case:social_media_quick_clip",
        "use_case:livestream_clip",
        "use_case:tutorial_demo"
      ]
    },
    {
      id: "cat:use_case:brand",
      label: { zh: "品牌/活动", en: "Brand & Events" },
      optionIds: ["use_case:brand_image", "use_case:event_recap", "use_case:city_travel_promo", "use_case:corporate_event"]
    }
  ]
```

Coverage: 7 + 4 + 4 = 15. All 15 use case options covered.

**File 4: `src/lib/prompt/options/format.options.ts`**

Add after the `options` array closing `]` (line ~159):

```typescript
  categories: [
    {
      id: "cat:format:vertical",
      label: { zh: "竖屏 9:16", en: "Vertical 9:16" },
      optionIds: [
        "format:vertical_3s",
        "format:vertical_5s",
        "format:vertical_10s",
        "format:vertical_15s",
        "format:vertical_30s",
        "format:vertical_60s"
      ]
    },
    {
      id: "cat:format:horizontal",
      label: { zh: "横屏 16:9", en: "Horizontal 16:9" },
      optionIds: ["format:horizontal_12s", "format:horizontal_30s", "format:horizontal_60s"]
    },
    {
      id: "cat:format:square",
      label: { zh: "方形 1:1", en: "Square 1:1" },
      optionIds: ["format:square_8s", "format:square_15s", "format:square_30s"]
    },
    {
      id: "cat:format:other",
      label: { zh: "其他比例", en: "Other Ratios" },
      optionIds: ["format:portrait_34_8s", "format:portrait_45_10s", "format:wide_219_15s"]
    }
  ]
```

Coverage: 6 + 3 + 3 + 3 = 15. All 15 format options covered.

Do NOT modify the `options` arrays themselves. Only add the `categories` field after each options array. Preserve exact existing formatting of all objects.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
All four large option sets (Style, Subject, Use Case, Format) have categories arrays. Every option in each set appears in at least one category. Category IDs follow cat:{setId}:{slug} convention. All labels are bilingual. Consumer term mappings from Plan 05-01 remain intact. TypeScript compiles and all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create platform config data file</name>
  <files>
    src/lib/prompt/platforms/platform-data.ts
  </files>
  <read_first>
- src/lib/prompt/types.ts (for LocalizedText type)
- src/lib/prompt/options/format.options.ts (for format option IDs to reference)
  </read_first>
  <acceptance_criteria>
- Directory `src/lib/prompt/platforms/` exists with file `platform-data.ts`
- File exports `PlatformConfig` interface (grep: `export interface PlatformConfig` in platform-data.ts)
- File exports `platformConfigs` constant array (grep: `export const platformConfigs` in platform-data.ts)
- Platform IDs present: `douyin`, `rednote`, `bilibili`, `wechat_channels` (grep each in platform-data.ts)
- Each platform has `recommendedFormats` array with 3 format option IDs
- All 7 unique recommended format IDs reference real options in format.options.ts:
  - format:vertical_10s, format:vertical_15s, format:vertical_5s
  - format:portrait_34_8s
  - format:horizontal_12s, format:horizontal_30s, format:horizontal_60s
- Platform labels are bilingual (zh and en)
- `npx tsc --noEmit` passes
  </acceptance_criteria>
  <action>

Create the directory and file:

```bash
mkdir -p src/lib/prompt/platforms
```

Create `src/lib/prompt/platforms/platform-data.ts` with this exact content:

```typescript
import type { LocalizedText } from "../types";

/** Platform-specific format recommendations for quick-entry tags. */
export interface PlatformConfig {
  /** Unique platform identifier */
  id: string;
  /** Display label (zh/en) */
  label: LocalizedText;
  /** Format option IDs recommended for this platform.
   *  Each ID references an option in format.options.ts.
   *  Only formats compatible with the current target tool are applied at runtime. */
  recommendedFormats: string[];
}

/** Domestic Chinese platform format conventions, based on common best practices as of 2026. */
export const platformConfigs: PlatformConfig[] = [
  {
    id: "douyin",
    label: { zh: "抖音", en: "Douyin" },
    recommendedFormats: ["format:vertical_10s", "format:vertical_15s", "format:vertical_5s"]
  },
  {
    id: "rednote",
    label: { zh: "小红书", en: "RedNote" },
    recommendedFormats: ["format:vertical_10s", "format:vertical_15s", "format:portrait_34_8s"]
  },
  {
    id: "bilibili",
    label: { zh: "B站", en: "Bilibili" },
    recommendedFormats: ["format:horizontal_12s", "format:horizontal_30s", "format:horizontal_60s"]
  },
  {
    id: "wechat_channels",
    label: { zh: "视频号", en: "WeChat Channels" },
    recommendedFormats: ["format:vertical_10s", "format:vertical_15s", "format:horizontal_12s"]
  }
];
```

No changes to `src/lib/prompt/init.ts` needed. The platform config file is a standalone data module imported directly by the UI component in Plan 05-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
Platform config file exists at src/lib/prompt/platforms/platform-data.ts with PlatformConfig interface and platformConfigs array containing 4 domestic platforms. Each platform recommends 3 valid format option IDs. All IDs reference real options in format.options.ts. TypeScript compiles and all tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compile-time data | Category definitions and platform config are TypeScript constants — no runtime user input reaches this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Category `optionIds` arrays in option files | accept | Category membership is compile-time data. Invalid option IDs would be caught at component render time (option not found in set). Config is author-maintained, not user-editable. |
| T-05-05 | Tampering | `platformConfigs` in platform-data.ts | accept | Format ID strings are compile-time constants. Runtime filtering by appliesTo (in Plan 05-03) prevents incompatible format selection. |
| T-05-06 | Information Disclosure | Platform config data | accept | Platform names and format recommendations are public knowledge. Client-side bundle exposure is acceptable for a static local tool. |
</threat_model>

<verification>
Phase-level verification after all tasks complete:
- `npx tsc --noEmit` passes
- `npm test` passes
- 4 option files contain `categories:` arrays with correct category counts
- `cat:style:*`, `cat:subject:*`, `cat:use_case:*`, `cat:format:*` IDs present
- All option IDs in each set appear in at least one category
- `src/lib/prompt/platforms/platform-data.ts` exists with `PlatformConfig` interface and `platformConfigs` array
- 4 platform entries: douyin, rednote, bilibili, wechat_channels
- All 7 recommended format IDs reference real options in format.options.ts
</verification>

<success_criteria>
- [ ] Style option set (18 options) has 4 categories covering all options
- [ ] Subject option set (15 options) has 4 categories covering all options
- [ ] Use Case option set (15 options) has 3 categories covering all options
- [ ] Format option set (15 options) has 4 categories covering all options
- [ ] Category IDs follow `cat:{setId}:{slug}` convention, labels bilingual
- [ ] Platform config file exists with PlatformConfig type and 4 platform entries
- [ ] Each platform recommends 3 valid format option IDs
- [ ] TypeScript compiles, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-consumer-translation/05-02-SUMMARY.md`
</output>
