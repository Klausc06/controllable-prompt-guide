---
phase: 01-safety-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompt/options/constraints.options.ts
  - next.config.ts
autonomous: true
requirements:
  - OPT-04
  - D-03
user_setup: []

must_haves:
  truths:
    - "Two new safety constraints (avoid_temporal_flicker, avoid_quality_keywords) exist in the constraints catalog with complete bilingual metadata and appliesTo: [\"seedance\"]"
    - "npx next build produces a fully static out/ directory with no server-side dependencies"
  artifacts:
    - path: "src/lib/prompt/options/constraints.options.ts"
      provides: "Extended constraint catalog with 2 new Seedance-specific options"
      contains: "avoid_temporal_flicker"
      contains: "avoid_quality_keywords"
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output: \"export\""
  key_links:
    - from: "avoid_quality_keywords.promptFragment"
      to: "Seedance prompt output"
      via: "OptionSet in constraints catalog"
      pattern: "promptFragment\\.zh.*fast.*cinematic.*epic"
    - from: "next.config.ts output export"
      to: "Build output"
      via: "next build CLI"
      pattern: "npx next build"
---

<objective>
Add two missing safety constraints from the official Seedance 2.0 prompt guide to the constraints option catalog. Enable static export with a one-line config change.

Purpose: Seedance 2.0 users need `avoid_temporal_flicker` (prevents flicker/frame jitter) and `avoid_quality_keywords` (prevents quality-killing keywords like "fast", "cinematic", "epic"). Static export converts the Next.js project into a zero-server static site deployable anywhere.

Output: Extended constraints catalog, static-export-configured Next.js build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-foundation/01-CONTEXT.md
@src/lib/prompt/options/constraints.options.ts
@src/lib/prompt/types.ts
@next.config.ts

<interfaces>
<!-- Key types the executor needs from the codebase -->

From src/lib/prompt/types.ts:
```typescript
export interface OptionSet {
  id: string;
  version: string;
  label: LocalizedText;
  options: OptionItem[];
}

export interface OptionItem {
  id: string;
  version: string;
  label: LocalizedText;
  plain: LocalizedText;
  professionalTerms: string[];
  promptFragment: LocalizedText;
  appliesTo: TargetToolId[];  // currently "seedance" | "generic_video"
  riskHint?: LocalizedText;
}

export interface LocalizedText {
  zh: string;
  en: string;
}
```

From next.config.ts (current):
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { reactStrictMode: true };
export default nextConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add avoid_temporal_flicker and avoid_quality_keywords to constraints.options.ts</name>
  <files>src/lib/prompt/options/constraints.options.ts</files>
  <action>
Append two new OptionItem entries to the `options` array inside the `constraintsOptions` constant. Follow the exact pattern of existing options (id, version: "0.1.0", label, plain, professionalTerms, promptFragment, appliesTo). Both new options are Seedance-specific — their `appliesTo` must be `["seedance"]` only (NOT `["seedance", "generic_video"]` — per D-02, these constraints come from the official Seedance guide and are not applicable to generic video models).

**Option 1: `avoid_temporal_flicker`**
- id: "avoid_temporal_flicker"
- version: "0.1.0"
- label.zh: "避免画面闪烁和跳变"
- label.en: "Avoid temporal flicker and frame jitter"
- plain.zh: "减少画面中的闪烁、跳变和不连贯帧，保持输出画面稳定"
- plain.en: "Reduce flicker, jitter, and inconsistent frames in the video output"
- professionalTerms: ["temporal consistency", "frame stability", "no flicker"]
- promptFragment.zh: "避免画面闪烁和跳变，保持帧间连贯一致"
- promptFragment.en: "avoid temporal flicker and frame jitter, maintaining consistent frame-to-frame coherence"
- appliesTo: ["seedance"]

**Option 2: `avoid_quality_keywords`**
- id: "avoid_quality_keywords"
- version: "0.1.0"
- label.zh: "避免降低输出质量的关键词"
- label.en: "Avoid quality-degrading keywords"
- plain.zh: "Seedance 2.0 对 'fast', 'cinematic', 'epic', 'amazing', 'lots_of_movement' 等关键词敏感，会降低输出质量"
- plain.en: "Seedance 2.0 is sensitive to keywords like 'fast', 'cinematic', 'epic', 'amazing', 'lots_of_movement' which degrade output quality"
- professionalTerms: ["prompt engineering", "keyword hygiene", "quality preservation"]
- promptFragment.zh: "避免使用 'fast', 'cinematic', 'epic', 'amazing', 'lots_of_movement' 等会降低 Seedance 输出质量的关键词"
- promptFragment.en: "avoid quality-degrading keywords such as 'fast', 'cinematic', 'epic', 'amazing', 'lots_of_movement'"
- appliesTo: ["seedance"]

Insert these after the existing `single_focal_subject` option (the last entry). Maintain the array structure and trailing comma consistency. The `constraintsOptions` const uses `satisfies OptionSet` so TypeScript will validate correctness.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Two new options exist in constraints.options.ts, typecheck passes, each has complete bilingual metadata with appliesTo: ["seedance"]</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enable static export in next.config.ts</name>
  <files>next.config.ts</files>
  <action>
Add `output: "export"` to the NextConfig object in next.config.ts.

The current config is:
```typescript
const nextConfig: NextConfig = {
  reactStrictMode: true
};
```

Change to:
```typescript
const nextConfig: NextConfig = {
  reactStrictMode: true,
  output: "export"
};
```

This is the only change. No additional Next.js configuration is needed — the project has no API routes, no middleware, no server components with dynamic behavior, and no `next/image` usage. The `"use client"` component (prompt-guide.tsx) will pre-render its static shell and hydrate client-side.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>`npx next build` completes without errors and produces an `out/` directory containing a fully static site (index.html + static assets)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| option catalog -> renderer | Constraint option data flows from static config to rendered prompt text |
| build output (out/) | Static site deployed to third-party hosting — no server-side validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | constraints.options.ts | mitigate | Options declared with `satisfies OptionSet` — TypeScript strict mode catches schema violations at build time |
| T-01-02 | Tampering | next.config.ts | mitigate | Configuration in version-controlled TypeScript — any invalid export config breaks `next build` |
| T-01-03 | Information Disclosure | Static export (out/) | accept | No secrets, no API keys, no backend — entire app is public static content by design |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero type errors after adding new options
2. `npm test` — all 8 existing tests continue to pass (new options don't break anything)
3. `npx next build` — produces `out/` directory with index.html and static assets, zero build errors
4. `ls out/` — verify index.html exists
</verification>

<success_criteria>
1. `constraints.options.ts` contains 9 options total (7 original + 2 new), both new options have `appliesTo: ["seedance"]` only
2. `npx next build` completes successfully and `out/index.html` exists
3. `npx tsc --noEmit` and `npm test` both pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-foundation/01-01-SUMMARY.md`
</output>
