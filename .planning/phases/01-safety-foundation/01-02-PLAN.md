---
phase: 01-safety-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompt/adapters.ts
  - src/components/prompt-guide.tsx
  - src/lib/prompt/validation.test.ts
autonomous: true
requirements:
  - ARCH-07
  - TEST-08
user_setup: []

must_haves:
  truths:
    - "Seedance prompt output contains safety constraint text when safety constraint options are selected"
    - "Deselecting a safety default constraint option produces an amber warning in the rendered output"
    - "Switching target tool auto-selects the new target safetyDefaults into the constraints selection"
    - "TEST-08 assertions cover positive (safety text present) and negative (warning on deselect)"
  artifacts:
    - path: "src/lib/prompt/adapters.ts"
      provides: "safetyDefaults awareness in renderPrompt"
      min_lines: 28
    - path: "src/components/prompt-guide.tsx"
      provides: "Auto-select safety defaults into constraints on target switch"
    - path: "src/lib/prompt/validation.test.ts"
      provides: "TEST-08 assertions for safety text and deselection warnings"
  key_links:
    - from: "adapters.ts renderPrompt"
      to: "TargetToolConfig.safetyDefaults"
      via: "brief.items constraint check"
    - from: "prompt-guide.tsx target switch"
      to: "TargetToolConfig.safetyDefaults"
      via: "setSelections constraints merge"
    - from: "validation.test.ts TEST-08"
      to: "renderPrompt via adapters.ts"
      via: "renderPrompt() call with selected constraint options"
---

<objective>
Inject safetyDefaults awareness into the renderer so missing safety constraints produce amber warnings. Auto-select safety defaults on target switch in the UI. Add TEST-08 assertions verifying safety text appears in Seedance output.

Purpose: ARCH-07 — TargetToolConfig.safetyDefaults (array of option IDs) must auto-select on target switch and trigger warnings if deselected. TEST-08 — assert Seedance output contains IP/commercial-safety constraint text.

Output: Modified adapters.ts with safety warnings, modified prompt-guide.tsx with auto-select, extended validation.test.ts with safety assertions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-foundation/01-CONTEXT.md
@src/lib/prompt/adapters.ts
@src/lib/prompt/types.ts
@src/lib/prompt/targets/seedance.target.ts
@src/lib/prompt/targets/generic-video.target.ts
@src/lib/prompt/renderers/video-renderer.ts
@src/lib/prompt/brief.ts
@src/components/prompt-guide.tsx
@src/lib/prompt/validation.test.ts

<interfaces>
From src/lib/prompt/types.ts — TargetToolConfig.safetyDefaults is string[] of option IDs. RenderedPrompt.warnings is LocalizedText[]. LocalizedText has zh and en string fields.

From src/lib/prompt/adapters.ts — renderPrompt() currently builds brief, routes to renderer via if/else, and returns directly. No safetyDefaults awareness.

From src/components/prompt-guide.tsx line 258 — target switch buttons use onClick={() => setTargetToolId(tool.id)}. No safetyDefaults merge.

From src/lib/prompt/validation.test.ts line 9 — completeSelections fixture has constraints: ["no_ip_or_celebrity", "stable_identity"]. Missing readable_text (Seedance safetyDefault).

Seedance safetyDefaults: ["no_ip_or_celebrity", "stable_identity", "readable_text"]
Generic safetyDefaults: ["no_ip_or_celebrity", "stable_identity"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject safetyDefaults awareness into adapters.ts renderPrompt</name>
  <files>src/lib/prompt/adapters.ts</files>
  <action>
Modify renderPrompt() in src/lib/prompt/adapters.ts to detect when target.safetyDefaults are missing from the user's constraint selections and add amber warnings.

Step 1 — Update imports: Add LocalizedText to the type import on line 4.

Step 2 — Restructure: Instead of returning directly from the if/else, assign the rendered result to a let variable:

  let rendered: RenderedPrompt;
  if (params.targetToolId === "seedance") {
    rendered = renderSeedancePrompt(brief, target);
  } else {
    rendered = renderGenericVideoPrompt(brief, target);
  }

Step 3 — After rendering, check safety defaults: Find the "constraints" BriefItem in brief.items. Extract selected option IDs into a Set. Filter target.safetyDefaults to IDs NOT in that Set. If any are missing, construct a LocalizedText warning and append to rendered.warnings:

  zh: "已取消预选的安全约束：{ids}。这可能影响生成结果的安全合规性。"
  en: "Safety defaults deselected: {ids}. This may affect output safety compliance."

Use the raw option ID strings in the warning (no need to look up labels via getOptionById).

Step 4 — Return rendered.

Important: Do NOT forcibly inject safety text into the prompt. Per D-01, respect explicit deselection — only add a warning. Auto-selection (Task 2) ensures safetyDefaults are pre-selected by default.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>renderPrompt() checks target.safetyDefaults against brief constraints, appends LocalizedText warning when safety defaults are missing. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-select safety defaults on target switch in prompt-guide.tsx</name>
  <files>src/components/prompt-guide.tsx</files>
  <action>
Modify the target tool selection buttons in PromptGuide component (around line 258). Per D-01, safetyDefaults MUST be auto-selected as constraints when user switches to a target.

Current handler:
  onClick={() => setTargetToolId(tool.id)}

Replace with:
  onClick={() => {
    setTargetToolId(tool.id);
    setSelections(current => {
      const currentConstraints = selectionArray(current.constraints);
      const merged = [...new Set([...currentConstraints, ...tool.safetyDefaults])];
      return { ...current, constraints: merged };
    });
  }}

Use the existing selectionArray helper (line 27) to normalize current.constraints. Use new Set for deduplication — re-selecting the same target must not duplicate entries. User's manually selected constraints are preserved (not removed).

Behavior: Clicking Seedance adds "no_ip_or_celebrity", "stable_identity", "readable_text" to constraints. Clicking Generic Video adds "no_ip_or_celebrity", "stable_identity". Task 1's renderer warning handles the case where user subsequently deselects a safety default.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Target switch buttons call setTargetToolId AND merge tool.safetyDefaults into constraints selection. Typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write TEST-08 verification assertions</name>
  <files>src/lib/prompt/validation.test.ts</files>
  <behavior>
    - Test A (positive): When constraints include no_ip_or_celebrity and stable_identity, Seedance zhPrompt contains safety fragments: "不使用明星脸", "影视 IP", "未授权角色", "品牌侵权"
    - Test B (negative): When constraints exclude all safety defaults, rendered.warnings contains warning referencing missing safety default IDs (no_ip_or_celebrity, stable_identity, readable_text)
  </behavior>
  <action>
Add two test cases to the existing describe block in validation.test.ts (after line 76, before closing });).

Pre-step: Update the completeSelections fixture — add "readable_text" to the constraints array so it includes all Seedance safetyDefaults:
  constraints: ["no_ip_or_celebrity", "stable_identity", "readable_text"],

This prevents existing tests from unexpectedly triggering the new safety warnings.

Test A — "includes safety constraint text in Seedance output (TEST-08)":
Render Seedance with completeSelections. Assert zhPrompt contains:
  - "不使用明星脸" (no celebrity faces)
  - "影视 IP" (no film/TV IP)
  - "未授权角色" (no unauthorized roles)
  - "品牌侵权" (no brand infringement)

Test B — "warns when safety defaults are deselected from Seedance output (TEST-08)":
Render Seedance with constraints: ["simple_scene"] (none of the safetyDefaults). Assert warnings.length >= 1 and warnings contain "安全约束", "no_ip_or_celebrity", "stable_identity", "readable_text".

Test A will pass immediately (current renderer already includes constraint text from selections). Test B will fail until Task 1 is implemented.
  </action>
  <verify>
    <automated>npx vitest run src/lib/prompt/validation.test.ts</automated>
  </verify>
  <done>Both TEST-08 assertions pass with all existing tests. Seedance output contains safety constraint text. Deselected safety defaults produce warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User selection state -> renderer | User can deselect safety constraints — renderer must detect and warn |
| target config -> component state | safetyDefaults IDs flow from static config into mutable UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | TargetToolConfig.safetyDefaults | mitigate | safetyDefaults in version-controlled TypeScript with satisfies type check; TEST-08 asserts safety text in output |
| T-01-05 | Tampering | User deselection of safety defaults | accept | D-01 allows deselection; amber warning informs but does not block |
| T-01-06 | Repudiation | Missing safety in prompt output | mitigate | warnings array in RenderedPrompt provides audit trail rendered as amber UI |
| T-01-07 | Elevation of Privilege | Static config injection | accept | safetyDefaults are option IDs from static catalog; no external input boundary crossed |
</threat_model>

<verification>
1. npx tsc --noEmit — zero type errors
2. npm test — all 10 tests pass (8 existing + 2 new TEST-08)
3. npm run lint — clean
4. Manual: Open app, select Seedance — verify constraints pre-selected with safety defaults. Deselect one — verify amber warning in output panel.
</verification>

<success_criteria>
1. renderPrompt() detects missing target.safetyDefaults and appends LocalizedText warning to rendered.warnings
2. Switching target auto-merges tool.safetyDefaults into constraints selection via setSelections in prompt-guide.tsx
3. validation.test.ts has two TEST-08 assertions: (A) safety text in zhPrompt, (B) deselected safety triggers warning
4. All 10 tests pass, typecheck passes, lint clean
</success_criteria>

<output>
After completion, create .planning/phases/01-safety-foundation/01-02-SUMMARY.md
</output>
